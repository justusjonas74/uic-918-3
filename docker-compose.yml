services:
  web-ui:
    build:
      context: .
      dockerfile: web/Dockerfile
    container_name: uic-918-decoder-web
    ports:
      - "8089:3000"
    environment:
      - TZ=Europe/Berlin
    restart: always
